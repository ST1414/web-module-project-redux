// Import


// Initial State
const initialState = {
    favorites: [],
    displayFavorites: false
}

// Reducer
const favReducer = (state = initialState, action) => {
    switch(action.type){

        default:
            return state;
    }
}

export default favReducer;